# Django + Angular 16 Assessment

## Setup Instructions

Open **two terminal tabs** in VS Code.

### Terminal 1: Django Backend
1. `cd backend`
2. `pip install -r requirements.txt`
3. `python manage.py runserver 8000`

### Terminal 2: Angular 16 Frontend
1. `cd frontend`
2. `npm install`
3. `npm start`
